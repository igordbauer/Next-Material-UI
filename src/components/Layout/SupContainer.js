import React from "react";
import { Box, Typography } from "@mui/material";
const SupContainer = (props) => {
  return (
    <Box sx={{ p: 0.5 }}>
      <Typography>sup</Typography>
    </Box>
  );
};
export default SupContainer;
